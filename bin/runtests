#! /usr/bin/env bash

# N.B. quicklisp must be installed and configured.

clisp -ansi -i init.lisp -norc --quiet \
    -x "(in-package :paip.$1)" \
    -x "(clunit:run-suite '$1-suite :report-progress nil)"
